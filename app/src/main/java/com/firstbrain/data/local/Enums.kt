package com.firstbrain.data.local

enum class Urgency { Low, Medium, High, Critical }

enum class TaskType { work, personal, learning, health, other }

enum class TaskStatus { pending, completed, skipped }

enum class InteractionAction { viewed, completed, skipped, snoozed, reopened }
